# UDOF Validation Runner Dependencies
# Core requirements
numpy>=1.20.0

# Required for this sandbox (many modules rely on it)
scipy>=1.7.0

# Required for SPARC per-galaxy fitter
pandas>=1.5.0

# Optional (for CMB power spectrum test)
camb>=1.3.0

# Optional (for σ₈ normalization and sound horizon - can use fixed values if unavailable)
# Install via: pip install classy-community
# Or build from source: https://github.com/lesgourg/class_public
# classy-community>=3.2.0

# Note: This sandbox is intended to be fully self-contained (no parent-project imports).

