# UDOF Validation Runner Dependencies
# Core requirements
numpy>=1.20.0

# Optional but recommended (for full functionality)
scipy>=1.7.0

# Optional (for CMB power spectrum test)
camb>=1.3.0

# Optional (for σ₈ normalization and sound horizon - can use fixed values if unavailable)
# Install via: pip install classy-community
# Or build from source: https://github.com/lesgourg/class_public
# classy-community>=3.2.0

# Note: Some tests require additional UDOF modules from parent project:
# - boltzmann_mcmc.udof_boltzmann_corrected (ProperCorrectedGrowth)
# - boltzmann_mcmc.ruthless_analysis.phase2_theory.response_model (R_X)
# - prime0.toe.sparc_campaign (SPARC separated formula)
# These will gracefully degrade if not available.

